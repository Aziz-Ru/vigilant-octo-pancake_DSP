# Causal System

A causal system is a system where the output at any time depends only on past and present inputs, not future inputs.

## Example of a Causal System

1. y(t)=x(t)+2x(t-1);

2. Non Causal y(t)=x(t+1)+x(t)

